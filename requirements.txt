# Functions Framework for Python 3 HTTP Cloud Functions
functions-framework==3.*

# Web requests
requests>=2.31.0

# Flask is included via functions-framework, but explicit version is safe
flask>=2.3.2

# Environment variable management
python-dotenv>=1.0.0

# Google Gemini / OpenAI LLM SDK
openai>=0.27.8

# Google Pytrends for trend scraping
pytrends>=4.9.0

# --- NEW: Image Generation & Publisher Updates ---
google-genai>=0.3.0    # Required for the new 'Nano Banana' (Gemini 2.5) script
pillow>=10.0.0         # Required for image processing (PIL)
pydantic>=2.0.0        # Required for error handling in the new script
